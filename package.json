{
  "name": "ebdapp",
  "version": "0.1.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js", /*im Terminal: "nodemon server*/
    "dev": "concurrently --kill-others-on-fail \"yarn client\" \"yarn server\""
  },
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^4.16.3",
    "mongodb": "^3.1.1",
    "mongoose": "^5.2.5"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
